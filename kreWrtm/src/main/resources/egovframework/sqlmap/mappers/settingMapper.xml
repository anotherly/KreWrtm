<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.hivesys.setting.mapper.SettingMapper">
    <select id="selectSettingValue" parameterType="string" resultType="string">
        SELECT SETTING_VALUE
        FROM TBL_SYSTEM_SETTING
        WHERE SETTING_KEY = #{_parameter}
    </select>

    <insert id="upsertSetting">
        INSERT INTO TBL_SYSTEM_SETTING (SETTING_KEY, SETTING_VALUE, SETTING_DESC)
        VALUES (#{settingKey}, #{settingValue}, '메인 대시보드 데이터 갱신 주기(초)')
        ON DUPLICATE KEY UPDATE
            SETTING_VALUE = VALUES(SETTING_VALUE),
            UPDATED_AT = CURRENT_TIMESTAMP
    </insert>
</mapper>
